{
  "name": "agent-harness",
  "version": "1.0.0",
  "private": true,
  "description": "Agent harness monorepo",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "start": "pnpm --filter api run start",
    "server": "pnpm --filter api run server",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  }
}
